-- PostgreSQL init script - database is created automatically with POSTGRES_DB env var
-- This file is kept for future initialization if needed
-- All extensions and schemas can be created here
CREATE SCHEMA IF NOT EXISTS public;
